{
  "name": "web-party",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Watch parties P2P para YouTube e Crunchyroll, sem backend.",
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check.js",
    "package": "node scripts/package.js"
  },
  "engines": { "node": ">=22" }
}
